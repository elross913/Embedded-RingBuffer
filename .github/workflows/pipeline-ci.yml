name: C++ CI Pipeline (Dockerized)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout du code
      uses: actions/checkout@v4

    - name: 1. Construction de l'image de l'usine
      run: make build_image

    - name: 2. Exécution de l'analyse et des tests dans Docker
      run: make test